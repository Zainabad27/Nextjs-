import React from 'react';
import Navbar from '@/components/CustomComponents/Navbar';

const page = () => {
    return (
        <main>
            <Navbar/>
         
        </main>
    );
};

export default page;